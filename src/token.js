function token(i){
    const characters ='ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';
    let t = '';
    for(let i = 0; length(i) <= 10; ++i){
        t += Math.random(characters);
        console.log(t);
    }
}

consol = token(i);
return consol;
